
server:
   port: 9002

spring:
   application:
      name: rm-info-service
   datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/msdb?useSSL=false
      username: root
      password: root     
   jpa:
      show-sql: true
      generate-ddl: true
      hibernate:
         ddl-auto: update
      database: mysql
          
eureka:
 client:
   serviceUrl:
     defaultZone: http://localhost:8761/eureka
 
 
 
 
 
 
 